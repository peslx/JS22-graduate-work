(() => {
  "use strict";
  (() => {
    const e = document.querySelector(".smooth-scroll"),
      o = document.getElementById("offer"),
      t = () => {
        (e.style.display = "none"), (e.style.opacity = 0);
      };
    t(),
      document.addEventListener("scroll", () => {
        window.scrollY >= o.offsetTop + o.offsetHeight
          ? ((e.style.display = ""), (e.style.opacity = ""))
          : window.scrollY <= o.offsetHeight - o.offsetTop && t();
      }),
      e.addEventListener("click", (e) => {
        console.dir(benefits),
          e.preventDefault(),
          window.scrollBy({ top: -window.scrollY, behavior: "smooth" });
      });
  })();
})();
